// Code generated by goa v3.22.6, DO NOT EDIT.
//
// concerts HTTP client CLI support package
//
// Command:
// $ goa gen concerts/design

package cli

import (
	concertsc "concerts/gen/http/concerts/client"
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"concerts (list|create|show|update|delete)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "concerts list --page 7358875578451970142 --limit 48" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		concertsFlags = flag.NewFlagSet("concerts", flag.ContinueOnError)

		concertsListFlags     = flag.NewFlagSet("list", flag.ExitOnError)
		concertsListPageFlag  = concertsListFlags.String("page", "1", "")
		concertsListLimitFlag = concertsListFlags.String("limit", "10", "")

		concertsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		concertsCreateBodyFlag = concertsCreateFlags.String("body", "REQUIRED", "")

		concertsShowFlags         = flag.NewFlagSet("show", flag.ExitOnError)
		concertsShowConcertIDFlag = concertsShowFlags.String("concert-id", "REQUIRED", "コンサートのUUID")

		concertsUpdateFlags         = flag.NewFlagSet("update", flag.ExitOnError)
		concertsUpdateBodyFlag      = concertsUpdateFlags.String("body", "REQUIRED", "")
		concertsUpdateConcertIDFlag = concertsUpdateFlags.String("concert-id", "REQUIRED", "更新するコンサートのID")

		concertsDeleteFlags         = flag.NewFlagSet("delete", flag.ExitOnError)
		concertsDeleteConcertIDFlag = concertsDeleteFlags.String("concert-id", "REQUIRED", "削除するコンサートのID")
	)
	concertsFlags.Usage = concertsUsage
	concertsListFlags.Usage = concertsListUsage
	concertsCreateFlags.Usage = concertsCreateUsage
	concertsShowFlags.Usage = concertsShowUsage
	concertsUpdateFlags.Usage = concertsUpdateUsage
	concertsDeleteFlags.Usage = concertsDeleteUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "concerts":
			svcf = concertsFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "concerts":
			switch epn {
			case "list":
				epf = concertsListFlags

			case "create":
				epf = concertsCreateFlags

			case "show":
				epf = concertsShowFlags

			case "update":
				epf = concertsUpdateFlags

			case "delete":
				epf = concertsDeleteFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "concerts":
			c := concertsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = concertsc.BuildListPayload(*concertsListPageFlag, *concertsListLimitFlag)
			case "create":
				endpoint = c.Create()
				data, err = concertsc.BuildCreatePayload(*concertsCreateBodyFlag)
			case "show":
				endpoint = c.Show()
				data, err = concertsc.BuildShowPayload(*concertsShowConcertIDFlag)
			case "update":
				endpoint = c.Update()
				data, err = concertsc.BuildUpdatePayload(*concertsUpdateBodyFlag, *concertsUpdateConcertIDFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = concertsc.BuildDeletePayload(*concertsDeleteConcertIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// concertsUsage displays the usage of the concerts command and its subcommands.
func concertsUsage() {
	fmt.Fprintln(os.Stderr, `コンサートサービスは音楽コンサートのデータを管理します。`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] concerts COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: オプションのページネーション付きで今後のコンサートを一覧表示します。`)
	fmt.Fprintln(os.Stderr, `    create: 新しいコンサートエントリーを作成します。`)
	fmt.Fprintln(os.Stderr, `    show: IDで単一のコンサートを取得します。`)
	fmt.Fprintln(os.Stderr, `    update: IDで既存のコンサートを更新します。`)
	fmt.Fprintln(os.Stderr, `    delete: IDでシステムからコンサートを削除します。`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s concerts COMMAND --help\n", os.Args[0])
}
func concertsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] concerts list", os.Args[0])
	fmt.Fprint(os.Stderr, " -page INT")
	fmt.Fprint(os.Stderr, " -limit INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `オプションのページネーション付きで今後のコンサートを一覧表示します。`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -page INT: `)
	fmt.Fprintln(os.Stderr, `    -limit INT: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "concerts list --page 7358875578451970142 --limit 48")
}

func concertsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] concerts create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `新しいコンサートエントリーを作成します。`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "concerts create --body '{\n      \"artist\": \"The Beatles\",\n      \"date\": \"2024-01-01\",\n      \"price\": 100,\n      \"venue\": \"The O2 Arena\"\n   }'")
}

func concertsShowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] concerts show", os.Args[0])
	fmt.Fprint(os.Stderr, " -concert-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `IDで単一のコンサートを取得します。`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -concert-id STRING: コンサートのUUID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "concerts show --concert-id \"3f2e3087-7968-469b-b0cd-07658e8a1491\"")
}

func concertsUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] concerts update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -concert-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `IDで既存のコンサートを更新します。`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -concert-id STRING: 更新するコンサートのID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "concerts update --body '{\n      \"artist\": \"The Beatles\",\n      \"date\": \"2024-01-01\",\n      \"price\": 100,\n      \"venue\": \"The O2 Arena\"\n   }' --concert-id \"74027be0-df7b-491f-b589-d3569d5800a9\"")
}

func concertsDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] concerts delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -concert-id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `IDでシステムからコンサートを削除します。`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -concert-id STRING: 削除するコンサートのID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "concerts delete --concert-id \"06742365-34dd-4a73-9317-eef02a4421f0\"")
}
